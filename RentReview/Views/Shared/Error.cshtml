@model List<Exception>

<section>
    <div>
        <ul>
            @foreach (var error in Model)
            {
                <li>@error.Message</li>
            }
        </ul>
    </div>
</section>